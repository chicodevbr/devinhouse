class Carro {
  constructor(modelo, ano, cor, placa) {
    (this.modelo = modelo),
      (this.ano = ano),
      (this.cor = cor),
      (this.placa = placa);
  }
}

class Retangulo {
  constructor(altura, largura) {
    (this.altura = altura), (this.largura = largura);
  }
}
